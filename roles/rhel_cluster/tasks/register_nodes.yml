---
# Register RHEL cluster nodes in BCM
#
# Uses the shared register_bcm_nodes role with provided categories.
# Categories are specified in the cluster_nodes data structure.

- name: Register nodes using shared role
  ansible.builtin.include_role:
    name: register_bcm_nodes
  vars:
    bcm_node_category_mode: "provided"
